import CartProvider from "./CartProvider"

export default {
  title: "General/Cart Provider",
  component: CartProvider,
}

const Template = (args) => <CartProvider {...args} />

export const Default = {
  args: {},
}
